import 'package:flutter/material.dart';

class BottomNavTablet extends StatelessWidget {
  const BottomNavTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
