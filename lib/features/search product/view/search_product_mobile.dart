import 'package:flutter/material.dart';

class SearchProductMobile extends StatelessWidget {
  const SearchProductMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
